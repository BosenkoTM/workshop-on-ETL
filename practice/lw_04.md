# Лабораторная работа №4. Анализ и обработка больших данных с Dask (ETL-пайплайн)

**Цель работы:** получить практические навыки работы с библиотекой Dask для построения базовых ETL-конвейеров (Extract, Transform, Load) при обработке больших массивов данных, не помещающихся в оперативную память. Изучить принципы «ленивых вычислений» (lazy evaluation), управление памятью и визуализацию ориентированных ациклических графов (DAG).

**Инструментарий:**
*   Язык программирования: Python 3.
*   Среда выполнения: Google Colab или Jupyter Notebook (локально).
*   Библиотеки: `dask`, `pandas`, `graphviz`.

**Рабочий файл:** результат выполнения работы сохранить в файл с именем `lab_04_variant_XX.ipynb` (где XX — номер вашего варианта).

---

## Задание 4.1. Построение ETL-пайплайна средствами Dask

В этой части необходимо выполнить базовую загрузку, предобработку и сохранение большого датасета, избегая переполнения оперативной памяти компьютера (ошибка `KilledWorker`).

### Шаг 1. Extract (Извлечение данных)
Настройте локальный кластер Dask с оптимальными параметрами для вашей среды (без жестких ограничений по памяти). Затем «лениво» загрузите датасет, соответствующий вашему варианту (см. раздел "Варианты заданий"). Данные на этом этапе не загружаются в оперативную память целиком.

```python
# Установка Dask с полным набором зависимостей и графов
!pip install "dask[complete]" graphviz

import dask.dataframe as dd
from dask.distributed import Client
from dask.diagnostics import ProgressBar

# Инициализация клиента Dask (Оптимизированные настройки без жесткого лимита памяти)
client = Client(n_workers=2, threads_per_worker=2, processes=True)
display(client)

# Чтение CSV файла (Extract)
# При необходимости укажите dtype для проблемных столбцов
df = dd.read_csv('путь_к_вашему_файлу.csv')

# Просмотр структуры (метаданных) датасета
df
```
*Обратите внимание на количество разделов (partitions) и типы данных. Фактическое чтение файла еще не произошло.*

### Шаг 2. Transform (Трансформация и очистка данных)
Проведите профилирование качества данных: вычислите процент пропущенных значений. Используйте метод `.compute()` **только** для получения финального небольшого результата (списка процентов). Затем удалите разреженные столбцы, не прибегая к вычислениям в памяти.

```python
# Подсчет пропущенных значений (построение графа вычислений)
missing_values = df.isnull().sum()

# Вычисление процента пропусков
mysize = df.index.size
missing_count = ((missing_values / mysize) * 100)

# Запуск реальных вычислений только для агрегированной статистики
with ProgressBar():
    missing_count_percent = missing_count.compute()

print(missing_count_percent)

# Формирование списка столбцов, где пропусков > 60%
columns_to_drop = list(missing_count_percent[missing_count_percent > 60].index)
print("Удаляемые столбцы:", columns_to_drop)

# ОПТИМИЗАЦИЯ: Ленивое удаление столбцов без вызова .compute()!
# Метод .drop() лишь записывает правило "не читать эти столбцы в будущем".
df_dropped = df.drop(columns=columns_to_drop)

# Проверка результата (вычислит только первые 5 строк первого блока)
df_dropped.head()
```

### Шаг 3. Load (Загрузка / Сохранение результатов)
Чтобы завершить ETL-цикл, сохраните очищенный Dask DataFrame обратно на диск. Сохранение данных в формате `parquet` или нескольких `csv` позволит Dask записать огромный массив данных по частям.

```python
# Сохранение результатов трансформации в формате Parquet (Load)
# engine='pyarrow' рекомендуется для высокой скорости работы
df_dropped.to_parquet('cleaned_dataset.parquet', engine='pyarrow')

# Либо сохранение в CSV (Dask создаст папку с набором файлов cleaned_data-00.csv и т.д.)
# df_dropped.to_csv('cleaned_data-*.csv', index=False)
```

---

## Задание 4.2. Визуализация направленных ациклических графов (DAG)

Dask составляет граф выполнения задач (DAG) перед тем, как начать вычисления. Используя декоратор `dask.delayed`, создайте логику из нескольких простых python-функций и визуализируйте план выполнения планировщика.

### 4.2.1. Визуализация простого DAG
Создайте простую цепочку отложенных вычислений с одним финальным узлом и визуализируйте её.

```python
import dask.delayed as delayed

def increment(i):
    return i + 1

def add(x, y):
    return x + y

# Создание отложенных объектов
x = delayed(increment)(1)
y = delayed(increment)(2)
z = delayed(add)(x, y)

# Визуализация графа
z.visualize()

# Запуск вычислений и получение результата
print("Результат вычисления DAG:", z.compute())
```

### 4.2.2. Визуализация многоуровневого (сложного) DAG
Постройте двухуровневый граф вычислений на основе списка данных (имитация map-reduce процессов).

```python
data = [1, 2, 3, 4, 5]

# Слой 1: Инкремент каждого элемента списка
layer1 = [delayed(increment)(i) for i in data]

def double(x):
    return x * 2

# Слой 2: Удвоение результатов из слоя 1
layer2 = [delayed(double)(j) for j in layer1]

# Финальная агрегация
total2 = delayed(sum)(layer2)

# Визуализация полного графа
total2.visualize()

# Запуск вычислений
result = total2.compute()
print("Итоговый результат:", result)
```

---

## Таблица вариантов (Исходные данные)

**Источник данных:**[Скачать датасеты на платформе envlab](https://envlab.ru/mod/assign/view.php?id=185).  
*Если датасет находится в архиве (`.zip`), его необходимо распаковать перед загрузкой в ETL-пайплайн.*

Выберите ваш набор данных в зависимости от номера варианта:


[Варианты на платформе envlab](https://envlab.ru/mod/assign/view.php?id=185). 

**Источник данных:**[Скачать датасеты (Яндекс.Диск)](https://disk.yandex.ru/d/fbPE3VNKYocd7g).  
*Если датасет находится в архиве (`.zip`), его необходимо распаковать перед загрузкой в ETL-пайплайн.*

Выберите ваш набор данных в зависимости от номера варианта:

| Варианты | Имя файла (Датасет для ETL) |
| :---: | :--- |
| **1, 7, 13, 19, 25** | `Parking_Violations_Issued_-_Fiscal_Year_2014__August_2013___June_2014_.csv` |
| **2, 8, 14, 20** | `Parking_Violations_Issued_-_Fiscal_Year_2015.csv` |
| **3, 9, 15, 21** | `Parking_Violations_Issued_-_Fiscal_Year_2016.csv` |
| **4, 10, 16, 22** | `Parking_Violations_Issued_-_Fiscal_Year_2017.csv` |
| **5, 11, 17, 23** | `UK Property Price official data 1995-202304.zip` |
| **6, 12, 18, 24** | `Austin, TX House Listings.zip` |

---

## Требования к отчетности и критерии оценки

Отчет сдается в виде ссылки на репозиторий GitHub/GitVerse, содержащий выполненный `.ipynb` файл с сохраненными выводами (output) ячеек и отображаемыми графами.

### Критерии оценки (Максимум 10 баллов)

| Критерий | Баллы | Описание |
|---|:---:|---|
| **Настройка среды (Extract)** | 1 | Корректная настройка Dask Client без переполнения памяти. Успешная потоковая загрузка данных (`read_csv`). |
| **Анализ данных (Transform)** | 3 | Вычислены проценты пропусков, использован `ProgressBar`, результат выведен на экран с помощью `.compute()`. |
| **Оптимизация и выгрузка (Transform & Load)** | 2 | Реализовано ленивое удаление разреженных столбцов. Очищенный датасет успешно сохранен на диск (`to_parquet` / `to_csv`). |
| **Визуализация DAG (DAGs)** | 3 | Построены и визуализированы графы для простых и сложных зависимостей (выводятся картинки `.visualize()`). |
| **Оформление** | 1 | Ноутбук логично структурирован, код читаем, название файла соответствует требованиям (`lab_04_variant_XX.ipynb`). |

### Контрольные вопросы
1. В чем главное отличие архитектуры `dask.dataframe` от классического `pandas.dataframe` в контексте обработки Big Data?
2. Что такое "ленивые вычисления" (lazy evaluation) и почему вызов метода `.compute()` следует откладывать на самый конец ETL-пайплайна?
3. Что такое DAG (Directed Acyclic Graph), как он формируется в Dask и какую роль играет в оптимизации планировщика задач?

